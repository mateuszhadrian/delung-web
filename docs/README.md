# Indeks dokumentacji — status plików

> Konwencja jak w hadrianm-web: każdy plik `.md` bezpośrednio w `docs/` ma tu
> wpis ze statusem. **Dodajesz nowy plik do `docs/`? Dopisz go tutaj.**
> Zmieniasz decyzję opisaną w którymś dokumencie? Zaktualizuj jego
> status/adnotacje i ten indeks.
>
> Podkatalog `design/` = referencje designów (eksporty HTML) — poza indeksem,
> patrz `design/README.md`.

## ✅ Aktualne — źródła prawdy

| Plik | Czego dotyczy |
| --- | --- |
| `delung-web-entrance-analysis.md` | **Analiza wejściowa** projektu delung.pl: decyzje podjęte (tabela §2 + D1–D8), architektura docelowa, różnice względem hadrianm.pl, schemat CMS, routing/SEO, ryzyka |
| `delung-web-creation-process.md` | **Instrukcja wykonawcza** budowy strony: Część A (checklista), Część B (Etapy 0–7 krok po kroku), Część C (flow mediów klienta — D1), Część D (opcje backupowe) |
| `daily-workflow.md` | **Codzienny proces pracy** (instrukcja operacyjna, od Etapu 1): feature branch → `/test` → PR → checki → merge → auto-deploy + prod smoke; przypadki specjalne (baseline'y, CMS, hotfix); nota o enforcement rulesetu (repo prywatne na Free) |
| `etap-4-prompty.md` | **Prompty startowe Etapu 4** (części 4.1–4.5, jedna sesja = jedna część): kontekst wspólny (lekcje Etapu 3, podział ról, definition of done), prompt per część, szablon promptu korekty po testach manualnych |
| `analiza-chrome-globalny.md` | **Mini-analiza części 4.1** (chrome globalny): decyzje portu navbara (sticky, warianty `plain`/`over`, 4 pozycje menu), menu mobile jako bottom sheet na `overlay.ts`, stopka `ft` z dewiacjami (IG desktop, polityka mobile, kontrasty AA), BackButton POZA chrome (decyzja: brak w designach, logika `data-back` uśpiona), kontrakty selektorów testów |
| `analiza-strona-glowna.md` | **Mini-analiza części 4.2** (strona główna): hero mobile/desktop (kadry + typografia SVG, h1 dla smoke), sceny przypięte oferta/realizacje bez GSAP (`home-scroll.ts`, motion-gate), zajawki z danych lokalnych + kolekcji realizacji (Modal/BottomSheet), opinie/kontrasty pod ratchet axe, banner `#contact` z antyscrapingiem D-CH5, plan testów i baseline'ów |
| `analiza-oferta-kategorie.md` | **Mini-analiza części 4.3** (/oferta/ + /kategorie/): treści oferty kluczowane slugami `categories.ts` (6 kategorii, `inne` bez treści), desktop ARIA tabs + animacje panelu w CSS (bez GSAP), mobile karuzela 3+1 wg gotchas, karta kategorii = 6 pre-renderowanych sheetów na `overlay.ts`, CTA realizacji z `categoryLabel()` bez deep-linku (filtr = 4.4), kontrasty pod ratchet axe, redirect /kategorie/ nietknięty |
| `analiza-proces-onas-polityka.md` | **Mini-analiza części 4.5** (/proces-wspolpracy/ + /o-nas/ + /polityka-prywatnosci/, po jednym PR na widok + PR porządkowy): navbar `over` w tonie ciemnym (`hdr dark` z eksportów — korekta ustaleń 4.1), duplikaty per-breakpoint zamiast relokacji JS, wspólny moduł danych opinii (`src/lib/opinie.ts`), pełny port layoutu polityki `pp-*` z przejściem antyscrapingu na `contact-details.ts`, kasacja SkeletonPage, domknięcie etapu (legacy-dark zostaje dla kontaktu, zacieśnienie LHCI) |
| `analiza-kontakt.md` | **Mini-analiza Etapu 5** (`/kontakt/` + formularz): port widoku na design delung (hero `over`, kafle kontaktowe wjeżdżające na hero, karta formularza), pole telefonu zamiast chipsów tematu, antyscraping na slotach `contact-details.ts` (koniec `[ POKAŻ ]`), kasacja `legacy-dark.css` i ambientu, nadawcy Resend na `send.delung.pl`, wyjście GSAP-a z projektu (Lenis na rAF — ~45 kB gz mniej), kroki w chmurze (Resend/Turnstile/KV/env) |
| `analiza-poprawki-wizualne.md` | **Runda poprawek po testach klienckich** (przed Etapem 6, decyzje D-P1–D-P6): kanoniczny deep-link kategorii `/oferta/#<slug>` (KOREKTA D-SG4), karty kategorii jako komponent współdzielony przez `/`, `/oferta/` i `/kategorie/` — tap w kafel na mobile otwiera sheet W MIEJSCU zamiast nawigować (KOREKTA D-OK3, rozszerzenie D-OK5), nieprzezroczysty hover CTA, desktopowe CTA procesu na stronie głównej (KOREKTA D-SG5), link do panelu opinii Google; podział na PR A/B i rachunek baseline'ów |
| `etap-6-poprawki-prompt.md` | **Prompt startowy rundy poprawek po Etapie 6** (do wklejenia w świeżej sesji): obowiązkowa lektura, stan projektu po Etapach 0–6, zasady twarde (brak commitów po stronie Claude'a, konwencja commitów, święta kolejność baseline'ów, antyscraping D-CH5), kontrakt testów, znane pułapki (szum runnera LCP, asymetria baseline'ów darwin/linux) i definition of done rundy |
| `poprawki-3-prompt.md` | **Prompt startowy rundy poprawek nr 3** (do wklejenia w świeżej sesji): obowiązkowa lektura, stan po Etapach 0–6 i dwóch rundach poprawek, zasady twarde (brak commitów po stronie Claude'a, konwencja commitów, kolejność sprzątania branchy, święta kolejność baseline'ów, antyscraping D-CH5), kontrakt testów oraz **katalog pułapek pomiarowych z rundy 2** (przejście `transform` fałszujące odczyt geometrii, elementy z `opacity: 0` łapiące kliknięcia, percepcyjny próg Playwrighta, zaokrąglanie długości wpisywanych z JS, technika wariantów przełączanych hashem na fizycznym urządzeniu, `gh run download` przy czerwonym CI) |
| `refactor-prompt.md` | **Prompt startowy refaktoru przed Etapem 7** (do wklejenia w świeżej sesji): obowiązkowa lektura, stan po Etapach 0–6 i trzech rundach poprawek (wraz z zacieśnionymi budżetami LHCI i zmierzonymi bajtami), definicja „refaktoru" w tym projekcie (zero zmian zachowania i wyglądu, baseline'y jako siatka bezpieczeństwa, lista kontraktów, których nie wolno złamać po cichu), zasady twarde, kontrakt testów, **katalog pułapek pomiarowych z rund 2 i 3** oraz narzędzia (sonda Playwrighta spoza repo, wyciąganie liczb LHCI z medianowych raportów CI). Sesja ma poprosić Mateusza o jego własny prompt z zakresem refaktoru, zanim cokolwiek zaproponuje |
| `analiza-etap-6.md` | **Mini-analiza Etapu 6** (SEO, pomiar, polish; decyzje D-E1–D-E11): komplet ikon = sam znaczek na białym kwadracie (`favicon.svg` jako prawdziwy wektor zamiast 43 kB base64), og-image = pełne logo w kadrze 1200×630 (`twitter:card` → `summary_large_image`), JSON-LD `FurnitureStore` na `/kontakt/` **bez `telephone`/`email`** (D-CH5 nietknięty, strażnikiem zostaje grep `dist/`) + `WebSite`/`Organization` na `/`, dane firmy w `src/lib/jsonld.ts`, Cloudflare Web Analytics przez auto-injection w Pages, pomiar przed/po i zacieśnienie budżetów LHCI osobnym commitem; kroki w chmurze, checklista telefonów, podział na PR A/B/C |
| `analiza-poprawki-2.md` | **Runda poprawek nr 2 — WYKONANA** (2026-08-04, PR #29–#33; decyzje D-Q1–D-Q6): **wyjście Lenisa z projektu** po sesji pomiarowej na fizycznym Macu (koszt = zdjęcie przycinane maską w kształcie liter w hero, przemalowywane w każdej klatce JS-owego scrolla; hero nietknięte, `--p` navbara wygładzane pętlą rAF), hero mobile przypinane `--hero-h` DOPIERO po wykryciu drgającego `100svh` (przeglądarki iOS zmieniające rozmiar webview) wraz z lekcją o zaokrąglaniu długości wpisywanych z JS, link opinii Google na Place ID (KOREKTA D-P6), szyna filtrów `/realizacje/` przyklejona przez całą listę (KOREKTA D-R7), scena realizacji przebudowana na siatkę z priorytetami kurczenia (przycisk nie zasłania treści na żadnej wysokości okna) oraz link „Więcej" otwierający detal zamiast nawigacji |
| `analiza-poprawki-3.md` | **Runda poprawek nr 3 — WYKONANA** (2026-08-04, PR #36–#39; decyzje D-T1–D-T4): napisy hero w Firefoksie rozjeżdżały się przy pierwszym wejściu (Gecko nie unieważnia rastra napisów SVG po podmianie kroju — biała kopia `<use>` i kopia przycięta maską zostają narysowane różnymi krojami), poprawka = **bramka na jawnym `document.fonts.load`**, która nie dopuszcza do rasteryzacji napisów krojem zastępczym; powrót listy `/realizacje/` na jej początek po zmianie kategorii (D-T2); pudełko opisu w scenie realizacji bierze wysokość z treści zamiast zgadywać ją z `vw` i scena dostała `row-gap` gwarantujący odstęp do CTA (D-T3/D-T4 — KOREKTA D-Q5); zacieśnienie budżetów LHCI do baseline'u po wyjściu Lenisa. W dokumencie także katalog pułapek pomiarowych tej rundy (`document.fonts.ready` jako sygnał fałszywy przy preloadowanych fontach, wstrzyknięcie CSS po `load` działające jak przemalowanie, nieskuteczność wymuszonego przemalowania przy żywej animacji, przeniesienie kontenera zapytań zmieniające rampy z reaktywnych na proaktywne) |
| `instrukcja-cms-prompt.md` | **Prompt startowy sesji dokumentacyjnej** (do wklejenia w świeżej sesji, PRZED Etapem 7): ma powstać instrukcja dodawania/edycji/usuwania realizacji przez panel `/admin` w dwóch wersjach — operatorskiej (`instrukcja-cms.md`) i nietechnicznej dla klienta (`instrukcja-panelu-klient.md`, pozycja nr 2 checklisty Etapu 7). Zawiera obowiązkową lekturę (config.yml jako źródło etykiet, schemat Zod, reguła CMS, Część C flow mediów), zakres merytoryczny (media, wideo z parą zdjęcie+poster, kolejność, usuwanie wraz z osieroconymi plikami w R2, co się dzieje po zapisie i ścieżka powrotu przy błędnym wpisie), **regułę antyhalucynacyjną** (sesja nie widzi UI Sveltii — etykiety cytowane z config.yml, wszystko inne oznaczone `⟦DO POTWIERDZENIA⟧`), plan zrzutów ekranu, plan treningu Mateusza z kryterium gotowości oraz pytania do rozstrzygnięcia przed pisaniem |
| `analiza-refactor.md` | **Audyt refaktoru przed Etapem 7 — WYKONANY W ZAKRESIE A–C** (2026-08-04, PR #42–#44; D/E/F odłożone — §13 i §14 niosą wyniki, pomiary i listę tego, co zostało nietknięte wraz z powodami): powtórzenia (5 modułów ruchu z ~220 zduplikowanymi liniami, bramka `js-motion` w 6 wrapperach, 12 klas kickera), struktura plików (dlaczego dużych `.astro` NIE dzielimy — utrata scope'owania stylów; jedyny wyjątek to sheet menu w `Navbar.astro`), martwy kod (3 tokeny bez konsumenta, ~105 linii helperów testów opisujących sekcje `about`/`audience` i trasę `/dla-kogo/` z szablonu, eksporty bez zewnętrznych konsumentów), **komentarze kłamiące o kontrakcie** (6 z 7 stałych `*_DESKTOP_MIN_PX` deklaruje ochronę testami, której nie ma — `proces-config.ts` nie jest importowany przez nic) oraz przegląd komentarzy z podziałem na duchy do kasacji (GSAP scrub, ScrollTrigger, `verify-hero.mjs`) i wiedzę pomiarową do zachowania. Zawiera strefy no-go, listę „rozważone i odrzucone", podział na 6 PR-ów wg rosnącego ryzyka i szablon promptu wdrożeniowego |
| `instrukcja-cms.md` | **Instrukcja operatorska panelu `/admin`** (dla Mateusza, PRZED szkoleniem klienta): model działania panelu (Sveltia → commit na `main` → build Cloudflare Pages → `delung.pl` w ~2 min; media osobnym kanałem do R2), logowanie kontem `delung-cms` i diagnostyka „panel nie wpuszcza", klucz R2 (decyzja: konfiguruje Mateusz raz przy przekazaniu), **pole po polu wg `config.yml`** z tabelą wymagane/opcjonalne zweryfikowaną wobec `content.schema.ts`, zdjęcia (HEIC, rozmiary przez `imgAt()`, metoda dobierania kadru `object-position` bez zgadywania), wideo (HandBrake, limity, **zasada pary zdjęcie+film w tej samej pozycji galerii**, zakaz uploadu poza polami), kolejność i kategorie (`order` steruje też trójką na stronie głównej; szyna filtrów ukrywa puste kategorie), edycja i podmiana zdjęcia, **usuwanie wraz ze sprzątaniem osieroconych plików w R2** (dokładna ścieżka w dashboardzie + kontrola testem), ścieżka błędnego wpisu zweryfikowana w `ci.yml`/`prod-smoke.yml` (produkcja zostaje na ostatniej dobrej wersji, ale publikacja staje) i katalog błędów, których walidacja NIE łapie, **plan treningu (6 ćwiczeń + kryterium gotowości)**, lista kontrolna `⟦DO POTWIERDZENIA⟧` do pierwszego przejścia przez panel oraz lista „to zawsze przez Mateusza" |
| `instrukcja-panelu-klient.md` | **Instrukcja panelu dla klienta** (PL, nietechniczna, do wydruku i na telefon; pozycja nr 2 checklisty Etapu 7): 8 kroków „chcę dodać realizację" z etykietami pól cytowanymi z `config.yml`, wideo w głównej ścieżce (HandBrake + zasada pary zdjęcie+film), przygotowanie telefonu (HEIC → „Najbardziej zgodne"), poprawianie kadru zamiast wymiany zdjęcia, kolejność i skutek dla strony głównej, usuwanie z przypomnieniem o plikach do sprzątnięcia, „gdy coś nie działa → zadzwoń" i lista czego nie ruszać. **Zawiera 14 placeholderów zrzutów** (`docs/media/cms-NN-*.png`) zebranych w listę do zrobienia za jednym przejściem oraz listę kontrolną — obie sekcje oznaczone „dla Mateusza, usunąć przed wydrukiem". **Dokument nie był weryfikowany przy otwartym panelu** — do zamknięcia listy kontrolnej traktować jak szkic |
| `remont-panelu-prompt.md` | **Prompt startowy sesji remontowej panelu** (do wklejenia w świeżej sesji, przed Etapem 7): zakres A–D — automatyczna miniatura wideo z klatki (**Cloudflare Media Transformations zweryfikowane na strefie: `/cdn-cgi/media/mode=frame,time=1s` zwraca JPEG z pliku w R2**, czyli bez Streamu i bez ffmpeg; „środek" liczony z istniejącego pola „Długość wideo"), kasacja pola „Kafel (cover)" na rzecz pierwszej pozycji galerii (**z pułapką różnych proporcji kadru kafla i galerii — dziś to dwa osobne pola nie bez powodu**), zakaz wideo na pierwszej pozycji (Zod `.superRefine` złapie to dopiero w buildzie — panel nie ma walidacji warunkowej) oraz diagnoza plakatu wideo (**poster i jego URL zweryfikowane jako poprawne — szukać w CSS, nie w CMS**). Zawiera obowiązkową lekturę, polecenie zebrania listy znalezisk Mateusza PRZED projektowaniem, zasady twarde, **kontrakt zamrożonej treści testów wizualnych** (`build:visual`, fixture, konsekwencje dla migracji schematu), znany błąd panelu Sveltii z obejściem oraz kolejność docs-first z definition of done |
| `analiza-remont-panelu.md` | **Remont panelu `/admin` — WYKONANY** (2026-08-05, przed Etapem 7, decyzje D-RP1–D-RP10): pozycja galerii przestaje być „zdjęcie + opcjonalny film" i staje się **wariantem** („Zdjęcie" albo „Film" — `types`/`typeKey`, mechanizm potwierdzony w binarium Sveltii 0.170.0), czyli wykluczenie obu mediów wymusza sam panel, a nie build; kasacja pola „Kafel (cover)" na rzecz pierwszej pozycji galerii (**bez `coverPosition`** — z pomiaru zastanych danych wynika, że dwóch różnych kadrów nie użyto ani razu), zakaz filmu na pierwszej pozycji przez `.superRefine` z komunikatem po ludzku, automatyczna miniatura filmu ze środkowej klatki (`videoFrameAt()` obok `imgAt()`; Cloudflare Media Transformations zmierzone: JPEG, cache 20 dni, czytelne 400 przy `time` poza długością klipu), etykiety parametrów wielkimi literami w CSS zamiast w danych, plan migracji treści produkcyjnej i fixture'u (w tym rozbicie wpisu klienta z filmem na pierwszej pozycji) oraz **nieudana próba odtworzenia usterki plakatu wideo** wraz z dwiema obalonymi hipotezami (format AVIF i klonowanie kadrów) |
| `analiza-realizacje.md` | **Mini-analiza części 4.4** (/realizacje/): filtry SSR+JS (puste kategorie ukryte, bez re-empty/paginacji), deep-link filtra `/realizacje/#<slug>` (domknięcie D-OK6, nadawcy z 4.3 podpięci), detal = JEDEN overlay `#work-detail` (CSS modal↔sheet przy 1024 — `sheetMQ`/760 znika, kasacja ui/Modal+BottomSheet), galeria snap/translateX + wideo na tap (preload=none, badge play, duration), projnav z kontekstem listy, kontrasty pod ratchet axe |
