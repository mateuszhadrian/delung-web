// Konfiguracja widoku /o-nas/ (część 4.5) — stała progu desktop W PARZE
// z @media w komponentach sekcji (CSS nie zaimportuje stałej — utrzymuj
// razem; reguła sections.md). Konsument: onas-motion.ts.
export const ONAS_DESKTOP_MIN_PX = 1024;
